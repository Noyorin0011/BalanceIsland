# Balance Island 后续开发任务书

## 2026-08-25 当前任务：套餐用量自动更新

用户已确认 ChatGPT 设置页能显示套餐用量，但 BalanceIsland 原有读取返回 HTTP 401，且成功数据只在手动点击“读取用量”后更新。目标是在不弱化现有 WebView 安全边界的前提下恢复读取，并增加默认关闭、明确警告的自动更新。

### 已验证结论

1. 旧脚本只用 Cookie 请求 `/backend-api/wham/usage`，在当前 ChatGPT 登录态返回 HTTP 401。
2. 同源同步请求 `/api/auth/session`，仅在 WebView JavaScript 内取得临时 `accessToken`，再以 `Authorization: Bearer` 请求 usage，可以成功读取；令牌不得返回原生层、写入配置、日志或错误信息。
3. MuMu 实测成功结果为 Plus、剩余约 7%，证明服务端数值会变化。当前响应只保存到 `primary_window`，但截图显示它是“每周使用限额”，不能继续把 primary 固定标成“5 小时”。恢复后要读取 `limit_window_seconds` 等字段动态判断窗口名称。
4. 当前页面代理 `10.0.2.2:7890` 可用，System WebView 版本为 `110.0.5481.154.1`。

### 实现要求

1. 保留已验证的 401 修复：先读取同源 session，再在页面内部携带 Bearer token 请求 usage；任何返回原生层的数据只能包含套餐类型、百分比、窗口时长、重置时间和更新时间。
2. 把用量模型由固定 primary/secondary 语义升级为按窗口时长解释：约 5 小时显示“5 小时额度”，约 7 天显示“周额度”；缺少或未知时长时使用中性“额度”名称，不猜测。
3. 增加自动更新开关，必须默认关闭并单独确认风险。推荐第一步实现“实验页面保持打开时立即读取并每 5 分钟更新”；如实现 15 分钟后台 WorkManager，必须先验证 CookieManager/代理链路，并满足下列额外边界。
4. 后台更新警告必须写明：会在后台使用等同密码的登录会话；非公开接口可能变化、限流、触发重新登录或账号风控；受信任代理仍可看到连接元数据，安装代理 CA 后可能看到会话；关闭开关或“断开并清除”会取消调度。
5. 401/403 不自动重试；网络错误最多交给 WorkManager 正常退避。失败时保留最后一次成功数据，并清楚标记更新时间、最近尝试时间与错误类别。
6. 防止重复请求：手动读取和自动读取必须共用单飞锁；成功回调后再安排下一次，不允许定时器堆积。
7. “断开并清除”必须同时取消自动更新、清除 Cookie/WebStorage/缓存/SSL 状态与本地用量；代理主机/端口可继续保留，但自动更新开关必须关闭。
8. 更新简体中文、繁體中文（台灣）、English、日本語、한국어，以及 `EXPERIMENTAL_FEATURES.md`、`README.md` 和 `CHANGELOG.md` 的未发布部分。

### 验收

1. 在当前 MuMu 登录态点击读取不再出现 401，缓存值与 ChatGPT“使用情况”页面的窗口类型、剩余百分比和重置时间一致。
2. 自动更新默认关闭；启用前出现明确警告；关闭或断开后不再请求。
3. 代理开启/关闭、页面重载、Activity 退出重进、会话过期、401/403、无网络和接口字段缺失均不会崩溃或泄露响应正文/令牌。
4. 五套翻译键一致；单元测试、`testDebugUnitTest`、`lintDebug`、`assembleDebug` 通过。
5. 使用并排包名 `com.noyorin.balanceisland.debug` 在 MuMu 验证，不覆盖正式版。最终撤销临时 `applicationIdSuffix`、调试日志与构建缓存，再审查 diff；未经用户明确要求不 push、不建 PR、不发布。

### 追加：浮岛套餐用量

1. 已缓存的套餐窗口可作为独立 OpenAI 条目加入浮岛轮播；没有 API 账户时也可单独显示。
2. 每个窗口显示“剩余百分比 / 距离下一次重置时间”，不把接口未提供的历史累计重置次数伪造成可用数据。
3. 套餐余量接入“浮岛显示”→“显示账户”：自动轮播与全部已配置 API 账户共同轮换；自定义分组允许 API 与套餐任意组合及套餐单独成组；固定套餐模式只显示套餐余量。读取成功、切换模式/分组或断开清除后，运行中的浮岛立即重绘。
4. 主页面预览、实际浮岛和五套语言文案保持一致，并在 MuMu 截图验收。
5. 增加默认关闭的套餐重置周期通知；启用时申请系统通知权限，只在成功读取确认旧周期已到期且新 `reset_at` 明显推进后发送，同一周期不重复，且不得为通知增加后台请求。

## 当前基线

继续完善现有 Kotlin/Compose Android 工程，不重写架构。当前 `0.8.4` 已实现 9 家服务商、多 Key 加密保存、每账户刷新间隔、HTTP 429 `Retry-After`/指数退避、查询串行与防抖、独立查询缓存、状态栏文字条、自动/固定/自定义 Provider 分组轮播、可调文字宽度与长文字平滑滚动、双层高对比文字、超薄背景样式、分级额度告警、异常变动报警、无变化自动隐藏与静默恢复提醒、手动余额、控制中心磁贴、后台恢复、OpenAI Project/Admin Key 分流、Xiaomi MiMo 普通 Key 校验、Google Gemini `AQ.`/`AIza` Key 原生校验，以及简体中文、繁體中文、English、日本語、한국어切换。

显示层只使用 Android 公开的 `TYPE_APPLICATION_OVERLAY`、WindowInsets、DisplayCutout 和 RoundedCorner API。不得加入厂商型号白名单、隐藏 SystemUI 反射或厂商专有卡片 SDK。

## 第一阶段：构建与多设备验证

1. 使用 JDK 17、Android SDK 35 构建 debug APK，修复编译与 Lint 错误。
2. 至少在三类设备验证：中置挖孔圆角屏、刘海屏、无挖孔或平板。
3. 验证左/右安全区和左/右贴边预设；若与状态图标重叠，记录所需微调值。
4. 验证简体中文、English、跟随系统三种语言；检查设置页、文字条、前台通知、额度通知和控制中心磁贴。
5. 验证透明文字、半透明底、描边边缘留白、自适应底色、跟随系统明暗文字、五种颜色、X/Y 滑块、多账户轮播及固定服务商。
6. 验证首次权限、启动/停止、任务划除、重启、应用升级和系统强行停止行为。
7. 验证每账户自动/自定义刷新周期、HTTP 429 自动退避、30 秒防抖、15 分钟 WorkManager 兜底、橙色/红色告警、下降步进通知与异常变动冷却。
8. 验证自动隐藏关闭时保持常驻；开启后超时隐藏，余额变化时恢复并只发静默通知；通知点击恢复文字条。
9. 验证切换服务商会清空备注和 Key；MiMo `sk-` Key 可校验，`tp-` Key 显示明确的不支持原因。
10. 验证自定义 Provider 分组只轮播所选账户、至少保留一个可用选项；分别以 `72/220/320dp` 检查短文字静止、长文字平滑循环滚动及左右定位。

验收标准：应用不崩溃；不显示未配置平台；Key 不泄漏；语言切换无残留硬编码文案；安全区在主要屏幕形态可用；多账户增删、迁移和轮播正确。

## 接口与安全

1. 在既有 HTTP 429 指数退避基础上，为 408/5xx 等可重试错误增加分类退避，但不重试 400/401/402/403 等确定性错误。
2. 区分无网络、超时、密钥无效、权限不足、余额不足、限流和服务端错误。
3. 实现 OpenAI Costs 分页，处理 `has_more` 和下一页游标。
4. 增加“删除全部凭据”，同时清理密文、账户索引、快照和 Keystore Alias。
5. 为告警状态机、下降步进、`SecureKeyStore` 迁移与删除补单元测试。
6. 公众发布前增加可选后端代理，避免在客户端保存 OpenAI Admin Key。

## 产品体验

1. 增加“固定指定账户”和“同服务商合计”模式。
2. 增加字体大小、背景透明度和最大宽度设置。
3. 增加余额变化趋势、桌面小组件和标准常驻通知。
4. 根据多设备截图优化 Insets 回退值，但不要按品牌或型号写死。
5. 新增语言时复制 `values-en/strings.xml`，保持所有资源键一致并执行 Android Lint。

## 可直接交给 Codex 的提示词

```text
请继续开发这个 Android 工程。先阅读 README.md 和 CODEX_TASK_SPEC.md，保持现有 Kotlin/Compose 架构。先用 Android SDK 35 构建 debug APK并修复编译问题；再检查中文、英文和跟随系统语言是否覆盖界面、文字条、通知与控制中心磁贴；最后在中置挖孔、刘海屏和无挖孔设备验证四种位置预设。不要加入厂商型号特判或专有流体卡片 SDK，不要把真实 API Key 写入代码、日志或测试夹具。
```
