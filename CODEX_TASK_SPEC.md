# 状态栏余额后续开发任务书

## 一、当前基线

继续完善现有 Kotlin/Compose Android 工程，不要重写架构。当前 `0.6.0-background-multi-provider` 已实现：

- 8 家服务商多 Key 加密保存与旧版 DeepSeek/OpenAI 单 Key 自动迁移。
- 每个账户独立查询和缓存。
- `真实服务商图标［备注/后四位］余额` 状态栏文字条（无外侧界定符）。
- 只显示已配置账户；自动轮播或固定服务商。
- 文字条运行时 1–1440 分钟自定义查询，默认 1 分钟。
- 逐账户警告线、警告线 +50% 橙色预警、越线红色与系统通知。
- 逐账户下降步进通知（默认 5）和手动显示余额。
- 可伸缩设置分类卡片。
- 左上/右上圆角安全区和贴边预设，X/Y 微调、颜色、透明底样式。
- PLC110 设备配置和可选 OPPO 流体云接入骨架。
- 控制中心磁贴一键启停，以及可选的服务回收、开机和应用升级自动恢复。

首要目标机：一加 Ace 5 至尊版 `PLC110`，ColorOS `PLC110_16.0.7.200`，屏幕 `1272 × 2800`。默认文字条高度 `26dp`、圆角侧边安全区 `28dp`、顶部偏移 `13dp`、最大内容宽度 `210dp`。

## 二、第一阶段：构建与真机验证（必须完成）

1. 使用 JDK 17、Android SDK 35 构建 debug APK，修复所有编译/Lint 错误。
2. 在 PLC110 上验证首次通知和悬浮窗授权、启动/停止、杀进程和锁屏恢复。
3. 分别截图四种位置预设，确认不会被圆角、中间挖孔、时间和网络图标遮挡。
4. 验证透明文字和半透明底、五种颜色、X/Y 滑块是否即时生效。
5. 添加以下账户组合并检查轮播：
   - 仅一个 DeepSeek，无备注：不显示方括号。
   - 两个 DeepSeek，无备注：分别显示 Key 后四位。
   - DeepSeek + OpenAI，有备注：显示对应备注。
   - 删除当前账户：列表和文字条立即移除该账户。
6. 验证点按立即切换、5 秒自动轮播、长按进入设置。
7. 验证 1 分钟刷新、修改刷新间隔即时生效，以及 ColorOS 熄屏/后台限制。
8. 用模拟或测试账户验证橙色、红色、越线推送和每下降 5 单位推送。
9. 验证手动余额在接口成功/失败时都能显示，并可清空恢复 API 值。
10. 根据真机截图调整 PLC110 默认侧边距和顶部偏移。

验收标准：应用不崩溃；不显示未配置平台；Key 不泄漏；四种位置可用；文字不与系统状态图标重叠；多账户增删、迁移、轮播均正确。

## 三、接口与安全

1. 为请求增加指数退避，但不重试 400/401/402/403 等确定性错误。
2. 区分无网络、超时、密钥无效、权限不足、余额不足、限流和服务端错误。
3. 实现 OpenAI Costs 分页，处理 `has_more`/下一页游标。
4. 增加“删除全部凭据”，同时清理密文、账户索引、快照和 Keystore Alias。
5. 为余额警告状态机、下降步进、`SecureKeyStore` 迁移与删除补单元测试。
6. 公众发布前增加可选后端代理，使 App 不直接保存 OpenAI Admin Key。

## 四、产品体验

1. 增加“固定指定账户”和“同服务商合计”模式。
2. 用真机安全区/窗口 Insets 改进静态 `28dp` 侧边距回退。
3. 增加字体大小、背景透明度和最大宽度设置。
4. 增加余额变化趋势、桌面小组件、标准常驻通知。
5. 真机验证控制中心磁贴、开机恢复和任务划除恢复在 ColorOS 16 的行为。
6. 增强 ColorOS 电池白名单/自启动引导，明确强行停止不可恢复。

## 五、可选 OPPO 原生流体云

只有在 OPPO 开放平台审批并取得已发布 UPK、`serviceId`、`eventCode` 后再实现：

1. 使用 `oppo-fluid-cloud/upk/src/pages/index.oml` 创建 Pantanal 服务工程。
2. 发布 UPK，新增独立 `fluidCloud` product flavor，引入 SeedlingSupportSDK。
3. 将示例 Provider 转入该 flavor，完成创建、更新、销卡。
4. 原生后端成功出卡后停止兼容文字层，避免重叠。

审批前 `ColorOsFluidCloudBackend` 必须保持不可用。不得反射隐藏 SystemUI API，也不得用媒体通知伪造流体云。

## 六、交付物

- 可重复构建源码。
- `app-debug.apk`、构建日志摘要、PLC110 四种位置截图。
- API 解析与凭据迁移单元测试。
- 更新后的 README 和真机兼容性记录。

## 可直接交给 Codex 的提示词

```text
请继续开发这个 Android 工程。先阅读 README.md 和 CODEX_TASK_SPEC.md，保持现有 Kotlin/Compose 架构。先用 Android SDK 35 构建 debug APK并修复编译问题；然后为多账户加密存储/旧版迁移和 DeepSeek/OpenAI 解析补测试；最后按任务书在 PLC110_16.0.7.200 上验证四种位置预设、多账户轮播及 ColorOS 后台行为。不要把真实 API Key 写入代码、日志或测试夹具。每次修改后重新构建并报告 APK 路径、测试结果和真机截图。
```
