# Balance Island v0.9.1

## 套餐用量读取与网络兼容

- 修复 ChatGPT/Codex 套餐用量接口仅携带 Cookie 时返回 HTTP 401；临时访问令牌只在 WebView 页面 JavaScript 内使用，不进入 Android 原生层、配置或日志。
- 新增默认关闭的应用内 HTTP 代理，只影响 BalanceIsland 的实验 WebView，不修改模拟器、系统或 API Provider 网络。
- 补充加载进度、25 秒超时、网络/HTTP/SSL 错误与 WebView 渲染进程恢复入口，减少白屏和无反馈问题。
- 按接口返回的窗口秒数识别约 5 小时、周或通用额度，不再按字段名猜测窗口类型。

## 自动更新与重置通知

- 新增默认关闭且需单独确认的五分钟自动更新，只在实验页面保持打开时运行，不建立后台会话轮询。
- 手动与自动读取共用单飞锁；401/403/429 会暂停自动更新，失败保留最后成功结果，断开、重载或销毁页面后忽略迟到回调。
- 新增默认关闭的重置周期通知：仅在成功读取确认旧周期已到期且新重置时间明显推进后发送，同一周期不会重复通知，也不会增加后台请求。

## 浮岛显示

- 套餐余量以 OpenAI 图标和“剩余百分比 / 重置倒计时”加入实际浮岛与主页面预览；没有 API 账户时也能单独显示。
- “显示账户”新增套餐编排：自动轮播同时包含全部已配置 API 账户和套餐余量；自定义分组可混合或仅选套餐；固定套餐模式只显示套餐余量。
- 读取、切换显示模式/分组或清除套餐数据后，运行中的浮岛会立即重绘。

## 安全与升级说明

- 登录会话仍只由本机 System WebView 管理；“断开并清除”会停止自动更新、使在途结果失效，并清除 Cookie、站点存储、缓存、历史、表单、SSL 状态与本地套餐数据。
- 套餐读取依赖非公开接口，可能随时变化、限流或要求重新登录；完整边界见 `EXPERIMENTAL_FEATURES.md`。
- 五套语言资源已同步至 266 个键；单元测试、Android Lint 和 Debug 构建均已通过。
- 正式 APK 延续现有发布证书，可从 v0.9.0 直接覆盖升级并保留应用数据；最低支持 Android 8.0（API 26）。
