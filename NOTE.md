# Balance Island v0.9.0

## 全新设置导航

- 设置页改为默认隐藏的侧边栏菜单，可从左侧边缘滑动或点击顶部标题打开。
- 概览、账户与提醒、浮岛显示、后台与语言不再挤在一列可折叠卡片中。
- 右下角新增浮岛启停按钮，图标会跟随当前运行状态。

## OpenAI 套餐用量（实验性）

- 新增独立警示色实验区，可在应用内登录 ChatGPT 后读取 Codex 套餐的 5 小时与周额度剩余百分比及重置时间。
- 此功能默认关闭，首次使用必须确认会话泄露、账号暴露、接口失效与重新登录等风险。
- 应用不会要求复制 Session ID/Cookie；登录会话只保留在本机 WebView，普通配置只保存套餐类型、百分比、重置时间和更新时间。
- “断开并清除”会删除该 WebView 的 Cookie、站点存储、缓存、SSL 状态与本地用量结果。
- 该读取方式使用非公开接口，不受 OpenAI 官方支持，可能随时变化或失效。详见 `EXPERIMENTAL_FEATURES.md`。

## 交互与稳定性

- “文字滚动区域宽度”继续保持拖动时逐 dp 实时保存和重绘，预览与实际浮岛宽度同步变化。
- 修复通知权限被临时撤销时可能发生的崩溃竞态。
- 统一应用内广播注册的导出限制，并补齐 Android 8.0/8.1 与快速设置 API 的兼容处理。
- 五套语言资源已同步，共 213 个 string key；Debug 构建、翻译校验与 Android lint 均已通过。

## 升级说明

- 正式 APK 延续 `v0.7.5` 以来的同一发布证书，可从现有正式版本直接覆盖升级并保留数据。
- 最低支持 Android 8.0（API 26）。
