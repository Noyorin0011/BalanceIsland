# Balance Island v0.9.2

## 主页面套餐余量预览

- “状态栏余额”顶部预览现在与实际浮岛使用同一显示选择；已配置 API 账户与 ChatGPT/Codex 套餐余量每 5 秒轮换，点按文字条可立即切换。
- 接受实验风险并成功读取后，概览页会显示套餐余量、重置倒计时与最后读取时间，并提供实验读取页和“浮岛显示”设置的直接入口。
- 套餐读取、更新或清除时，主页面和运行中的浮岛通过同一应用内事件立即刷新。
- 自动联网边界不变：仍仅在实验页面保持打开且用户单独启用自动更新时每 5 分钟读取；主页面和浮岛只读取本地筛选后的套餐字段。

## 发布与文档

- 从本版本起，正式 Release 除签名 APK 外同时上传 `BalanceIsland-v0.9.2-source-minimal.zip`。
- 最小源码包只包含 `app/src/main`、Gradle 构建文件、启动脚本与必要许可声明；不包含测试源码、任务资料、交接记忆、GitHub 配置、缓存、构建产物或签名材料，并会在上传前独立构建验证。
- 新增完整英文版 `README_EN.md`；中英文 README 顶部可互相切换，功能、构建、安全说明和官方接口逐节对应。

## 兼容性与验证

- 五套应用语言资源同步至 268 个键。
- 16 项单元测试、Android Lint、Debug 构建及最小源码独立构建均已通过。
- 正式 APK 延续现有发布证书，可从 v0.9.1 直接覆盖升级并保留应用数据；最低支持 Android 8.0（API 26）。
