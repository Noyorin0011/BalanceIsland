# 实验性功能与安全说明

## ChatGPT/Codex 套餐用量

BalanceIsland v0.9.0 首次加入可选的 ChatGPT/Codex 套餐用量读取。该功能适合不想频繁切换到其他应用查看 5 小时与周额度的用户，但它不是 OpenAI 提供的公开集成能力。

OpenAI 官方 API 文档目前公开的是 API 组织的模型调用用量与成本端点，例如 `GET /organization/usage/completions` 和 `GET /organization/costs`，并未提供 ChatGPT/Codex 订阅套餐剩余百分比 API：

- https://developers.openai.com/api/reference/python/resources/admin/subresources/organization/subresources/usage

本功能因此只能在用户主动登录 `https://chatgpt.com/` 后，由同一个 WebView 页面先读取同源 `/api/auth/session`，再使用页面内的临时访问令牌请求未公开的 `/backend-api/wham/usage`。访问令牌只存在于这段页面 JavaScript 的局部变量，不会返回 Android 原生层、写入应用配置或打印到日志。上述路径、响应结构和可用性都不受保证，OpenAI 可以随时修改或关闭它们。

## 启用方式

1. 从设置页左侧边缘滑动，或点击顶部标题打开侧边栏。
2. 进入带警示色的“实验性功能”区域。
3. 阅读风险说明并主动勾选确认。
4. 在应用内 WebView 登录 ChatGPT。
5. 回到 HTTPS `chatgpt.com` 页面，点击“读取用量”。

该功能不会要求用户粘贴 Session ID、Cookie 或 Authorization 值。

### 可选自动更新

“本页打开时每 5 分钟自动更新”默认关闭，并有独立风险确认。启用后，如果上次成功数据已经超过 5 分钟，页面会先读取一次；此后仅在实验页面保持打开且加载正常时，每次成功或可重试失败的回调结束后再安排下一次读取。它不是后台服务或 WorkManager 任务。

手动与自动读取共用单飞锁，不会同时发起多次请求。HTTP 401/403（会话失效或权限不足）与 429（限流）会暂停本次页面会话内的自动更新，不会自动重试；网络、HTTP 或解析失败会保留上次成功数据，并记录最近尝试时间与错误类别。关闭实验页面、关闭开关或点击“断开并清除”都会取消定时任务。

### 可选重置周期通知

“套餐额度进入新重置周期时通知”默认关闭。启用时，Android 13 及以上会请求系统通知权限。一次成功读取只有同时满足“上次记录的重置时间已经到达”和“本次重置时间明显向后推进”才会被视为新周期，从而避免首次读取、普通刷新、时钟秒级偏差或服务端提前调整时间造成误报。每次成功保存的新重置时间会成为下一次比较基准，因此同一周期不会重复通知。

该通知复用手动读取或本页自动读取的结果，不增加任何网络请求。由于本应用不会在后台持有 WebView 登录会话持续轮询，实验页面关闭期间无法实时发现重置；再次打开页面并成功读取后，仍可识别此前已经发生的新周期。

### 浮岛显示

成功读取后，回到“浮岛显示”→“显示账户”即可编排套餐余量：自动轮播会包含全部已配置 API 账户和已读取的套餐余量；自定义滚动分组可把 API 账户与套餐余量任意组合，也可只选套餐余量；“固定 ChatGPT/Codex 套餐余量”只显示套餐余量。套餐用量是带 OpenAI 图标的独立条目，没有 API 账户时也能单独显示。每个已知窗口使用“窗口名称 剩余百分比 / 距离重置时间”的格式，例如 `周 94% / 6天12时`。如果同时返回 5 小时和周窗口，两者会在同一条中显示；缺少窗口时长时使用中性名称。

接口提供的是下一次重置时间或剩余秒数，并不提供可信的历史累计“重置次数”，因此浮岛显示的是重置倒计时，不会推测累计次数。倒计时由本地时间计算，不会为了数字递减而增加网络请求。读取成功、切换显示模式/分组或清除用量时，正在运行的浮岛会立即重绘。

### 页面超时与应用内代理

如果页面直连超时，可在同一页面勾选“为此 WebView 使用代理”，填写主机和端口后点击“应用并重新加载”。代理默认关闭；当前只支持不带用户名和密码的 HTTP 代理。这里填写的是代理端点，ChatGPT 页面本身仍必须使用 HTTPS。

- 常见 Android 模拟器可用 `10.0.2.2` 访问电脑宿主机，默认端口为 `7890`。不同模拟器的宿主机地址可能不同，电脑上的代理还可能需要允许局域网连接。
- 开发测试时也可先执行 `adb reverse tcp:7890 tcp:7890`，再把主机填为 `127.0.0.1`；ADB 或模拟器重连后需要重新建立反向映射。
- 该配置使用 AndroidX WebKit 的进程级 WebView 代理覆盖，只影响 BalanceIsland 进程内的 WebView；它不会修改系统/模拟器全局代理，也不会代理 DeepSeek、OpenAI API 等 Provider 请求。
- 页面会明确显示当前是直连还是代理、System WebView 版本，以及主页面网络错误、HTTP 错误、SSL 错误、25 秒超时或渲染进程退出。SSL 错误不能忽略或继续访问。

代理服务器至少能看到连接目标、时间和流量元数据。如果设备安装并信任了代理提供的根证书，代理还可能解密 HTTPS，从而看到登录会话和页面内容。只使用自己控制或完全信任的代理，不要把公共代理用于账号登录。

## 本机保存的数据

应用自己的 SharedPreferences 只保存：

- 风险确认版本；
- 套餐类型；
- 各额度窗口的剩余百分比与窗口秒数；
- 对应重置时间；
- 上次成功读取时间；
- 自动更新开关，以及最近尝试时间和错误类别。
- 重置周期通知开关。
- 浮岛套餐显示模式与自定义分组选择。

登录 Cookie 和站点存储由 Android System WebView 管理，不会被复制到 BalanceIsland 的普通配置、日志、通知或浮岛文本中。应用全局关闭 Android 备份，避免配置和 WebView 数据进入系统云备份。

## 已采取的保护

- 实验功能默认关闭，并需要显式风险确认。
- 实验 Activity 不可被其他应用直接启动，也不会出现在最近任务中。
- 登录页启用 `FLAG_SECURE`，系统截图与最近任务缩略图会被阻止。
- WebView 禁止文件访问、content 访问和 HTTP 混合内容，并启用 Safe Browsing。
- 第三方 Cookie 默认关闭。
- 用量读取只允许在 HTTPS 且主机严格等于 `chatgpt.com` 时执行。
- session 访问令牌只在 WebView JavaScript 内用于下一次同源请求；传给原生层的响应会先筛选为套餐、窗口百分比、时长和重置时间。
- 没有使用 `addJavascriptInterface`，网页无法直接调用应用对象。
- 登录 Cookie 和站点数据由系统 WebView 保存在 Balance Island 的应用私有目录中；离开页面时会显式刷新 Cookie 存储，不会把 Cookie 复制到应用配置或共享存储。
- 错误提示不会显示接口响应正文、Cookie 或其他会话数据。
- 代理地址仅保存开关、主机和端口，不保存代理账号或密码；不支持在主机字段内填写 URL、路径或凭据。
- 自动更新默认关闭，只在实验页面打开时运行；手动与自动读取共用单飞锁，401/403/429 会暂停自动读取。
- 重置周期通知默认关闭，仅复用成功读取结果，并要求旧周期已到期且新重置时间明显推进。
- “断开并清除”会停止自动更新、使尚未返回的读取结果失效，并删除 Cookie、WebStorage、缓存、历史、表单、SSL 状态和本地用量记录，同时撤销风险确认。

## 仍然存在的风险

- 登录会话本质上等同密码。设备被控制、系统 WebView 存在漏洞或站点本身被篡改时，仍可能造成信息泄露或账号暴露。
- 嵌入式登录可能触发验证码、SSO 兼容问题、安全提醒或重新登录。
- 未公开接口可能返回不同字段、被限流、拒绝 WebView 请求或彻底移除。
- 套餐窗口名称与时长可能变化；应用按接口返回的窗口秒数识别约 5 小时或约 7 天，缺少或未知时长时只显示中性的“额度”。
- BalanceIsland 无法保证第三方网页加载的脚本、身份提供商或网络服务的行为。
- 代理是新的信任边界：恶意代理、代理日志、错误安装的根证书或共享代理配置都可能暴露连接信息乃至登录会话。

如果不能接受这些风险，请不要确认启用。查看失败时不要将 Cookie、Session ID、网页调试内容或完整响应发送给任何人。

## 清除与恢复

点击“断开并清除”后，实验功能会回到未确认状态；下次使用需要重新确认风险并登录。为方便排障，代理开关、主机和端口会保留，可在退出前手动关闭代理并点击“应用并重新加载”。卸载应用会删除包括代理配置在内的本机数据，但为了清除实验会话，不需要卸载正式版本。
