# Changelog

Balance Island 的版本变化统一记录在本文件中，新版本排列在前。

## 0.8.3

### 新增

- 添加 API Key 时可为该账户单独指定 `1–1440` 分钟刷新间隔；填 `0` 使用 Provider 自动建议值。
- 账户设置中可随时修改独立刷新间隔，不影响其他服务商或 Key。
- 自动策略默认建议：DeepSeek 1 分钟，OpenAI 5 分钟，OpenRouter/SiliconFlow/Moonshot 2 分钟，仅验证 Key 的服务商 15 分钟。
- 新增“跟随系统明暗文字”样式：浅色系统自动使用深色文字，深色系统自动使用白色文字，不使用描边。

### 限流保护

- HTTP 429 会优先遵循服务端 `Retry-After`，同时使用指数退避自动延长下一次请求，最长 24 小时；查询成功后自动清零退避级别。
- 限流期间保留上一次成功的余额或用量，不再直接把状态栏覆盖成“查询失败”，并显示预计自动重试时间。
- 界面、前台悬浮服务与 WorkManager 的查询在进程内串行执行；手动刷新加入 30 秒防抖，避免多个入口同时重复消耗限额。
- 新增账户只测试刚保存的 Key，不再顺带刷新全部已有账户。
- 描边文字增加左右绘制留白，修复滚动区域边缘的最后一个字/阴影被裁切。
- “自适应对比底色”改为约 `22dp` 高、`6dp` 圆角的紧凑小圆角矩形，并缩小左右内边距与阴影，不再呈现厚重胶囊效果。

### 文档与兼容

- 明确 Xiaomi MiMo 控制台可以查看余额与用量，但普通 API Key 没有公开余额/账单查询端点，应用继续使用手动余额。
- 旧账户自动使用 Provider 建议周期，无需迁移；既有全局刷新设置改为调度检查周期。
- 同步更新简体中文、繁體中文（台灣）、English、日本語和 한국어，共 `183` 个 string key。

## 0.8.2

### 新增

- “显示账户”新增自定义滚动分组，可勾选多家 Provider，仅轮播这些服务商下已配置的账户。
- 新增 `72–320 dp` 文字滚动区域宽度设置，不包含固定显示的服务商图标。
- 账户备注、Key 后四位与余额合并为单行内容；超出设定宽度时平滑循环滚动，短文字保持静止。

### 兼容与交互

- 既有自动轮播和固定 Provider 模式保持不变，自定义分组作为独立模式保存。
- 首次进入自定义分组时默认选中当前已配置的全部 Provider，且不会允许取消最后一个当前可用选项。
- 删除所选服务商的全部账户后会安全回退到现有账户列表，避免悬浮窗空白；重新配置后仍保留分组偏好。
- 点按文字条继续立即切换账户，长按继续打开设置；位置、安全区、描边和自适应底色逻辑不变。
- 同步更新简体中文、繁體中文（台灣）、English、日本語和 한국어，共 `177` 个 string key。

## 0.8.1

### 修复

- Google Gemini 兼容 Google AI Studio 新版 `AQ.` Authorization Key，同时保留旧版 `AIza` Standard Key。
- Gemini Key 清洗器可从带说明文字、引号或 `Bearer` 包装的内容中正确提取 `AQ.` Key。
- Gemini 校验改用原生 `GET /v1beta/models` 与 `x-goog-api-key` 请求头，不再依赖 OpenAI 兼容层。

### 文案

- Gemini 输入提示更新为 `AQ... / AIza...`。
- 明确免费层与后付费账户没有固定“剩余余额”，无需强制设置手动余额；预付费用户可从 Google AI Studio“用量和限制”页面抄入可用余额。
- 同步更新简体中文、繁體中文（台灣）、English、日本語和 한국어说明，资源键数量保持一致。

## 0.8.0

### 新增

- 新增“自动隐藏与静默提醒”：可设置余额连续 `5–1440` 分钟无变化后隐藏状态栏文字条，默认关闭。
- 隐藏期间余额发生变化时，文字条自动恢复，并在低优先级通知频道发送无横幅、无声音的静默摘要。
- 通知点击可同时打开设置页并恢复文字条；Android 13+ 未授权通知权限时会安全跳过通知。
- 新增每账户“异常变动报警”，支持绝对变动阈值、相对百分比阈值或任一阈值触发。
- 异常报警支持每账户独立冷却时间，默认 `1440` 分钟；首次刷新只建立比较基线，不会误报。
- 新增 Xiaomi MiMo：普通按量 `sk-` Key 通过官方模型列表验证，余额使用手动设置；明确拒绝仅限指定编程工具的 Token Plan `tp-` Key。

### 改进

- 异常变动状态与账户设置使用兼容旧版本的可选 JSON 字段，升级时自动采用安全默认值。
- 自动隐藏只隐藏悬浮视图，不停止前台服务、不清除运行意图，既有分钟刷新与 WorkManager 兜底保持不变。
- 变化检测复用现有刷新结果，不增加网络请求；通知只显示服务商及备注/Key 后四位，不泄漏完整 API Key。
- 切换待添加的服务商时会清空备注、API Key 和“显示 Key”状态，避免跨服务商误填凭据。
- 简体中文、繁體中文（台灣）、English、日本語和 한국어新增文案保持一致，共 `173` 个 string key。

### 签名与升级

- README 明确标注签名冲突已解决：从 `0.7.5` 起正式 Release 固定使用同一发布证书，CI 会拒绝 Android Debug 证书。
- 安装过早期 Debug 签名版本的用户仍需卸载一次后安装正式版；之后的正式 Release 可以直接覆盖升级。

## 0.7.5

### 新增

- 应用主界面现在自动跟随 Android 系统的浅色/深色模式。
- 新增完整浅色配色：浅色页面背景、白色卡片、深色正文、适配后的辅助文字与输入控件颜色。

### 改进

- 状态栏与导航栏背景、图标明暗会随系统主题同步切换。
- 冷启动阶段的 Android XML 主题也区分浅色和深色，减少启动时的深色闪屏。
- 账户卡片、设置分区、警告提示、删除按钮、加载指示器和状态信息改用 Material 主题语义颜色。
- 状态栏余额文字条的自适应描边/底色逻辑保持独立，不受应用页面主题影响。
- 正式 Release 改为上传 Release JKS 签名的精简 APK，并在发布前拒绝 Android Debug 证书。

### 兼容性

- 最低支持版本保持 Android 8.0（API 26）。
- 系统切换浅色或深色模式后，应用界面会随配置变化自动重组，无需重新配置 API Key。

## 0.7.4

- 修复 Kimi/Moonshot 被错误归类为“仅验证 Key”的问题，普通 Key 现在直接查询可用、现金与代金券余额。
- 自动兼容 Moonshot 国内人民币端点与国际美元端点。
- OpenRouter Management Key 读取各 Key 的官方 `usage_daily` 并与账户余额组合显示。
- OpenAI Admin Key 增加当天组织 Cost。
- DeepSeek、Moonshot、SiliconFlow 增加持久化“今日约用”统计。
- 保存当天最早余额、最后余额和已观察充值；进程当天被杀后，恢复时补算离线期间的余额下降。
- 新增“仅余额”“今日已用/余额”“自动轮播”三种显示内容模式。
- 同步更新简体中文、繁體中文（台灣）、English、日本語和 한국어资源。

## 0.7.3

- 新增“自动对比描边”：根据最终文字颜色自动选择黑色或白色轮廓，提升透明文字在复杂背景上的可读性。
- 新增“自适应对比底色”：亮色文字使用半透明深底，暗色文字使用半透明浅底。
- 告警状态仍优先使用橙色和红色，描边与底色会根据告警后的最终颜色重新计算。
- 设置页预览与实际悬浮文字条使用相同的对比逻辑。
- 功能不读取屏幕内容，不需要录屏或无障碍权限。
- 同步更新简体中文、繁體中文（台灣）、English、日本語和 한국어资源。

## 0.7.2

> 本版本曾以 Debug Preview 形式提供，仅用于兼容性和功能验证。

- 状态栏文字条的垂直位置恢复为从屏幕顶边计算：`0 dp` 即屏幕顶边，不再额外叠加状态栏居中偏移。
- 位置设置文案明确显示为“距屏幕顶部”，并同步更新五套语言资源。
- GitHub Actions 接入 Android 签名 Secrets，可同时产出 Debug APK 与 Release APK。
- 修复控制中心/快速设置磁贴在服务停止或进程被回收后仍卡在“运行中”的问题。
- 新增 `BUILDING.md`，说明 Android Studio、命令行、GitHub Actions 与自有 JKS 的构建方式。
- README 增加测试阶段提示及 AI 辅助开发声明。

## 0.7.1

- OpenAI 账户配置页新增 Project Key / Admin Key 权限提示。
- 输入 `sk-proj-` 时即时提醒：仅校验 Key，余额需要手动设置。
- `sk-proj-` 与普通 OpenAI Key 改为通过 `/v1/models` 校验，不再错误调用组织级账单接口。
- `sk-admin-` 继续用于查询组织消费与消费上限。
- 保存前自动清理 Key 前的 `Bearer`、说明文字、引号和多余空白，不修改 Key 本体。
- 清洗并测试仍失败时弹窗显示验证失败原因。
- 新增日本語、繁體中文（台灣）与 한국어，连同简体中文和 English 共支持五种语言。
- Android 13+ 系统应用语言与应用内语言切换均声明 `ja`、`zh-Hant-TW`、`ko`。
- 修复 Compose `weight` 错误导入和旧版系统快捷设置 Intent 常量的编译问题。
- GitHub Actions 使用 JDK 17 与 Android SDK 35 构建 APK。

## 0.7.0

### 新增与调整

- 删除特定手机型号和系统版本识别，默认支持 Android 8.0 及以上设备。
- 安全区位置改为读取系统状态栏、显示缺口和顶部圆角 Insets，并保留通用回退边距。
- 完全移除厂商流体卡片后端、UPK 模板和对应文档。
- 新增简体中文、English 和跟随系统语言，可在应用内即时切换。
- 设置页、状态栏文字条、前台服务通知、额度通知和控制中心磁贴均使用本地化资源。

### 当时仍需真机验证

- 中置挖孔、刘海屏、无挖孔和平板上的四种位置预设。
- 不同厂商省电策略下的分钟级刷新和自动恢复。
